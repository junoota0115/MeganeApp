<h1>Meganes</h1>
<table border=1>
<tr>
    <th>id</th>
    <th>title</th>
    <th>name</th>
    <th>content</th>
</tr>
<% @meganes.each do |megane|%>
<tr>
    <td><%= megane.id %></td>
    <td><%= megane.title %></td>
    <td><%= megane.name %></td>
    <td><%= megane.content %></td>
    <td><%=link_to '詳細',megane_path(megane) %></td>
    
</tr>
<%end%>

<%=link_to 'create',new_megane_path %>
</table>
